{
  "$schema": "https://turbo.build/schema.json",
  "globalPassThroughEnv": ["TEST_DATABASE_URL", "TEST_DATABASE_ADMIN_URL"],
  "tasks": {
    "build": { "dependsOn": ["^build"], "outputs": ["dist/**", ".next/**"] },
    "dev": { "dependsOn": ["^build"], "cache": false, "persistent": true },
    "lint": {},
    "test": {}
  }
}
